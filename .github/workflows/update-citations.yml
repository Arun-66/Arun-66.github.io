name: Update Google Scholar Citations (disabled)
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Legacy al-folio workflow — disabled'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled."
